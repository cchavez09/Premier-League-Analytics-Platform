# =======================================================
# FUTSTAT PROJECT — UNIFIED REQUIREMENTS INSTALLER
# =======================================================
# Handles setup for:
#   - Backend (Node.js / Express)
#   - Frontend (React)
#   - Python (Data / ML)
# =======================================================

# -------------------------------------------------------
#  SYSTEM REQUIREMENTS
# -------------------------------------------------------
# PostgreSQL 12+
# Node.js 16+
# Python 3.8+
# npm or yarn package manager

# -------------------------------------------------------
#  BACKEND (Node.js / Express)
# -------------------------------------------------------
cd backend
npm install express
npm install cors
npm install dotenv
npm install pg
npm install node-fetch@2
npm install --save-dev nodemon
cd ..

# -------------------------------------------------------
# FRONTEND (React)
# -------------------------------------------------------
cd frontend
npm install react
npm install react-dom
npm install react-scripts
npm install axios
npm install react-router-dom
npm install --save-dev @testing-library/react
npm install --save-dev @testing-library/jest-dom
npm install --save-dev @testing-library/user-event
cd ..

# -------------------------------------------------------
#  PYTHON DEPENDENCIES (Data / ML Scripts)
# -------------------------------------------------------
pip install pandas
pip install numpy
pip install scikit-learn
pip install joblib
pip install psycopg2-binary
pip install requests
pip install beautifulsoup4
pip install lxml
pip install python-dotenv

# -------------------------------------------------------
#  ENVIRONMENT VARIABLES (Manual Setup)
# -------------------------------------------------------
# Backend (backend/pginfo.env)
# HOME_API_KEY=your_football_data_api_key
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=futstat
# DB_USER=postgres
# DB_PASSWORD=your_password
# PORT=5001   # Change if 5000 unavailable

# Frontend (frontend/.env)
# REACT_APP_API_URL=http://localhost:5001

# -------------------------------------------------------
#  OPTIONAL UTILITIES
# -------------------------------------------------------
# To start backend (development mode):
# cd backend && npm run dev
#
# To start frontend:
# cd frontend && npm start
#
# To initialize database:
# cd data/scripts
# python pullAllData.py
# python createTableData.py
#
#
# Creation of Cloud
# npx neonctl@latest init (root directory)
